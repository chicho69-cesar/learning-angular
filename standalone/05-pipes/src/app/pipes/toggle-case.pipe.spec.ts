import { ToggleCasePipePipe } from './toggle-case.pipe';

describe('ToggleCasePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ToggleCasePipePipe();
    expect(pipe).toBeTruthy();
  });
});
