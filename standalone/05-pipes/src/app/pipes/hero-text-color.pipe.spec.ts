import { HeroTextColorPipePipe } from './hero-text-color.pipe';

describe('HeroTextColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HeroTextColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
